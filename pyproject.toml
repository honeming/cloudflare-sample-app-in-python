[project]
name = "awwbot"
version = "1.0.0"
description = "A Discord bot hosted on Cloudflare Workers, written in Python"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]
